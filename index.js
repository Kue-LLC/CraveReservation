/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import ReservationPage from './src/pages/ReservationPage';

AppRegistry.registerComponent(appName, () => ReservationPage);
